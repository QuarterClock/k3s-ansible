---
- name: Remove kernel module
  community.general.modprobe:
    name: iscsi_tcp
    rpi_state: absent

- name: Remove dependencies
  ansible.builtin.apt:
    name:
      - dmsetup
      - cryptsetup
      - nfs-common
      - open-iscsi
    rpi_state: absent
    purge: true
